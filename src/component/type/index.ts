export interface chatListType {
  id: number,
  name:string,
  time:string
}

export interface messageType {
  code: string
  content: string
  animation: number
}

export interface codeSpanType {
  content: string
  color:string
}